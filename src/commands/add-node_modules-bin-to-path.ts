import { getTargetUser } from "../os/user/target-user.ts";
import { LineInFile } from "./common/file-commands.ts";

export const addNodeModulesBinToPath = new LineInFile(
  await getTargetUser(),
  "~/.bashrc",
  "export PATH=$PATH:./node_modules/.bin",
);
