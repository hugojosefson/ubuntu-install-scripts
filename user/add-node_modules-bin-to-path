#!/usr/bin/env bash

set -e
cd "$(dirname "${0}")"
. ../sys/common.bash

# shellcheck disable=SC2016
grep -x 'export PATH=$PATH:./node_modules/.bin' ~/.bashrc || echo -e '\n''export PATH=$PATH:./node_modules/.bin' >> ~/.bashrc
