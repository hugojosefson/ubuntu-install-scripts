#!/usr/bin/env bash

set -e
cd "$(dirname "${0}")"
. ./common.bash

isInstalled google-chrome-unstable && exit 0

ensureInstalled https://dl.google.com/linux/direct/google-chrome-unstable_current_amd64.deb
